{
    "name": "taskmaster-electron",
    "version": "0.1.0",
    "private": true,
    "main": "main.js",
    "type": "module",
    "scripts": {
        "dev": "electron .",
        "start": "electron .",
        "build:sdk": "esbuild src/sdk/index.ts --bundle --format=esm --platform=browser --outfile=src/sdk/index.bundle.js --log-level=info --external:fs --external:stream --external:path --external:buffer"
    },
    "devDependencies": {
        "electron": "^31.2.0",
        "esbuild": "^0.23.1",
        "typescript": "^5.9.2"
    }
}